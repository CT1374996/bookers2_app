<h2>Users</h2>
<table>
  <tr>
    <th>image</th>
    <th>name</th>
    <th>link</th>
  </tr>
  <tr>
    <% users.each do |user| %>
    <td><%= image_tag profile_image.get_image %></td>
    <td><%= @user.name %></td>
    <td><%= link_to user_path %></td>
    <% end %>
  </tr>
</table>