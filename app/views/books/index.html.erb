<h2>Books</h2>
<table>
  <tr>
    <th>image</th>
    <th>Title</th>
    <th>opinion</th>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= image_tag book.user.get_profile_image(100, 100) %></td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
  </tr>
  <% end %>
</table>